# {Workstation_Name} CLAUDE.md

*Replace `{Workstation_Name}` with the account or theme name. Scopes Cowork's behaviour when this workstation is loaded. Inherits root `CLAUDE.md`.*

---

## Identity

This workstation is for **{Workstation_Name}**. It owns:

- {What this workstation owns}

It does not own:

- {What lives elsewhere}

When routed here, this is primary context. Root `CLAUDE.md`, `MEMORY.md`, `00_Resources/`, and the workstation's own `MEMORY.md` are loaded. Everything else is read-on-demand.

---

## Resources

| Resource | Read when... |
| --- | --- |
| `Resources/{file}.md` | ...{trigger condition} |

---

## Workflow

Numbered steps for the primary task this workstation supports.

1. {Step 1}
2. {Step 2}
3. {Step 3}

---

## Editorial Rules

Follow voice principles in `00_Resources/voice-principles.md`. Then layer:

- {Rule 1}
- {Rule 2}

---

## Notes

- Meeting notes: `Notes/{YYYY-MM-DD - Topic}.md`.
- Active projects: `Projects/`.
- Contacts: `People/` or canonical pages in root `05_People/`.
